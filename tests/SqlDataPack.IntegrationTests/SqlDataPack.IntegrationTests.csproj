<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Matches the library's shipping target frameworks so both builds are actually executed. -->
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <IsPackable>false</IsPackable>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>SqlDataPack.IntegrationTests</AssemblyName>
    <RootNamespace>SqlDataPack.IntegrationTests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="xunit" />
    <PackageReference Include="Xunit.SkippableFact" />
    <PackageReference Include="xunit.runner.visualstudio">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Testcontainers.MsSql" />
    <PackageReference Include="Microsoft.Data.SqlClient" />
    <PackageReference Include="Microsoft.Data.Sqlite" />
    <PackageReference Include="Microsoft.SqlServer.DacFx" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Force Microsoft.Data.SqlClient onto its managed networking stack on Windows. By default it
      uses native SNI there, whose socket connects bypass the managed System.Net.Sockets layer and
      are therefore invisible to NetworkIsolationTests' EventListener-based monitor. Linux always
      uses managed networking, so this only changes Windows; it keeps the no-network regression
      check meaningful cross-platform. TDS behaviour is otherwise equivalent.
    -->
    <RuntimeHostConfigurationOption Include="Switch.Microsoft.Data.SqlClient.UseManagedNetworkingOnWindows" Value="true" Trim="false" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Fixtures/*.sql" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/SqlDataPack/SqlDataPack.csproj" />
  </ItemGroup>
</Project>
